CREATE SEQUENCE IF NOT EXISTS partners_id_seq START WITH 1 INCREMENT BY 1;

CREATE TABLE partners
(
    id   BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    no   VARCHAR(25),
    name VARCHAR(255)
);